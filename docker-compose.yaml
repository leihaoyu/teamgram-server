version: "3"

services:
  teamgram:
    build: .
    restart: always
    network_mode: host
    mem_limit: 1280m

networks:
  teamgram_net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16
